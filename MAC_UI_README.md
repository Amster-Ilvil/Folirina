## v1.2.3 实验模式真实页可见结果 / GUI 显式启用修复

- “显示实验模式”改为“显示并启用实验模式”：勾选后自动切换到 `aligned_overlay_reveal`，避免仍停在 Auto 导致结果原样保留日文。
- 按真实页放宽实验路线的配准、文字连通域和面积阈值；默认仍 `enabled=false / allow_in_auto=false / require_explicit_mode=true`。
- 默认 `erase_source=hybrid`，但 SOURCE 背景仍不得成为彩色 TARGET 的像素权威。
- 白容器恢复改为局部 ROI 计算，避免 hybrid 在高分辨率真实页逐区域全页连通域分析造成卡顿。
- 单区域面积超限改为 REVIEW 并交给 Reveal，而不是直接丢失候选；配准失败诊断现在同时记录实测指标与阈值。
- 对用户提供的真实 SOURCE/TARGET 页验证：实验路线 accepted，18 个区域实际应用，最终彩图保持 TARGET 背景并出现中文文字。

## v1.2.2 实验 · 整页对齐挖洞显中文

- “迁移方式”新增 **显示实验模式**。默认关闭，因此正常用户不会看到或误选新路线。
- 打开后可选择“实验 · 整页对齐挖洞显中文”；界面常驻提示对齐误差/彩色露底风险，结果默认进入复核。
- REVIEW / REJECT 区域继续通过工作台“使用候选区域…”进入现有 Reveal，不新增第二套人工编辑器。
- 仅显示实验模式不会让 Auto 自动使用它；Auto 仍需配置中额外显式放开。

## v1.2.0 工作台 / 人工补漏架构统一

- Qt 工作台只负责交互；人工补漏提交由无 Qt 依赖的 `manual_review_service.py` 统一执行。
- `final.png / final_reviewed.png / final_auto.png` 由 `result_state.py` 统一管理，工作台不再维护第二套结果同步逻辑。
- Web Review 保存文字/匹配时采用非破坏合并，不会删除 Qt 中已经保存的 Reveal/人工补漏区域。
- 旧工程异常 bool/dict/list 状态继续通过统一 schema 兼容层进入工作台，不在各按钮事件里零散判断。

## v1.0.3 自动候选开放式文字

- 人工补漏卡片新增“使用候选区域…”，直接使用自动检测但因安全策略未自动覆盖的开放式文字候选框。
- 窄窗口下仍保持响应式布局；新增按钮不会再次把右侧面板撑宽。

# Manga HD Transfer Studio · macOS v0.8.21

本地 ZIP 版本，未上传 GitHub。GUI 已从上一版 Tk 界面重写为 **PySide6 / Qt**，视觉语言参考 KCC-Kindle-CHS：白色卡片、极细蓝灰边框、低饱和淡蓝强调色、紧凑控件、大预览区和右侧参数卡。

## v0.8.21 页面管理行为调整

- 页面配对完成后不再运行任何“自动检查无气泡/文本框”任务；所有页面默认正文，封面/扉页/目录/插图等由缩略图人工快速标记。
- “恢复正文”只删除手动分类，不再触发后台扫描。
- 正文页处理时如果真实中文 OCR/气泡证据确认没有可迁移气泡或文本框，程序会原样写出高清日文页；这属于“无需替换”，不是删页。
- 手动跳过页始终写入 `final/`，整册输出页数不会因为跳过替换而减少。
- 缩略图只加载视口附近页面，并在滚动停止后以小批次解码；已经缩放的缩略图进入有限 LRU 缓存，显著减少长漫画滚动卡顿。

## v0.8.19 缩略图页面管理

- 第一页默认是缩略图画廊，每页可直接看内容和类型色标；可一键在高清日文/旧中文缩略图之间切换。
- 双击缩略图或列表行进入左右并排大图检查，支持翻页、滚轮缩放、拖拽、适合窗口和 100%。
- 缩略图/列表都支持多选与右键快速分类；页面类型不再依赖文件名判断。
- 右侧详情持续显示分类来源、自动判断原因和配对/气泡统计；页面标记继续作为处理准入条件。
- 大图状态更新采用轻刷新，批量进度变化不会反复读取全分辨率图片。

## v0.8.18 页面管理与停止按钮

- 顶部工具栏新增常驻红色“■ 停止”，无论当前在页面管理、工作台还是出版输出都能停止当前后台任务。
- 第一页改为页面管理：配对与“是否进入精准蒙版”分离；支持多选页面批量标记封面、扉页、目录、单话首页、插图/纯图片等。
- 默认“配对后自动检查无气泡 / 文本框页面”。自动检查不做 OCR，只用配准和成对差异几何；自动跳过页直接保留高清日文原图。
- 自动判断失败时默认继续处理，不会因为分类器异常而漏页；任何自动结果都可用“正文 / 需替换”手动覆盖。
- 标记保存在 `page_management.json`，并参与断点续跑准入，旧缓存不能覆盖新的手动标记。


## 启动

双击 `启动_Manga_HD_Transfer.command`。

首次启动会建立项目内 `.venv` 并安装核心 + GUI 依赖。不会自动下载 PaddleOCR、LightGlue、LoFTR、LaMa、MangaLens、SAM 2 等模型权重。

也可以在环境准备完成后双击 `Manga HD Transfer Studio.app`。

## 四个页面

- **页面**：旧中文版 / 高清日文版 / 输出目录、自动页面配对、当前处理模式。
- **识别与配准**：Apple Vision / PP-OCRv5 / Sidecar，SIFT / LightGlue / LoFTR，白底气泡 / MangaLens / Sidecar。
- **替换工作台**：大图预览 + 右侧紧凑参数栏；日文原图、旧中文版、最终结果、复核标注和替换蒙版绑定同一当前页，并提供上一页/下一页连续浏览。
- **出版输出**：最终图、工程 JSON、QA、蒙版迁移图层、ORA/PSD。

## v0.5+ 成对差异精确替换

对于同源或近乎同源的日文/旧中文版页面，纯蒙版替换会优先：

`整页配准 → paired-diff → 只找真正被汉化过的气泡/文本框 → 目标实例吸附 → pixel-exact 覆盖`

未发生翻译变化的气泡、拟声词和画面不会被迁移。配准置信度不足时自动回退到通用 OCR/MangaLens/结构匹配路径。

真实页面回归指标见 `docs/REAL_PAIR_ACCEPTANCE_V05.md`。

## macOS

- Python 3.11+
- PySide6 / Qt 原生桌面 UI
- Apple Silicon 友好
- 优先使用 SF Pro Text / PingFang 系统字体
- 后台线程执行处理，避免 UI 主线程阻塞
- 高清日文母版永不覆盖

## Apple Live Text OCR

需要时双击 `首次安装_Apple_Live_Text_OCR.command`（兼容入口 `首次安装_Apple_Vision_OCR.command` 也会执行同一流程）。默认使用 Swift VisionKit Live Text，Helper 不可用时自动回退 `ExtractText` 快捷指令；不下载 OCR 模型权重。

## v0.6 批量与 MPS

- 整册后台队列：真实进度、缓存命中、失败继续、安全取消；
- 断点续跑：完成页直接跳过，插入前面页面不会让后续 target-stem 缓存整体失效；
- cheap-first 配准：同源/同版缩放优先 phase 快速路线，困难页才升级 SIFT / LightGlue / LoFTR；
- Apple MPS：LightGlue、LoFTR、MangaLens 与可选 Torch 局部超分统一设备策略；
- 模型整册常驻，不再逐页反复初始化；
- GUI 模型页只做浅探测，不会因为打开页面就 import 模型或下载权重。

如需 MPS 运行库，双击 `首次安装_MPS_AI加速.command`。


## 摄影旧版（v0.8）

- Mac GUI 默认优先 Apple Vision OCR，避免先下载 OCR 模型。
- 手机翻拍/反光/倾斜页面自动进入 `photo_pair`：高清日文页负责几何，旧中文版只提供译文/字形。
- 中文直接层使用“光照归一化像素 → 墨迹重建 → OCR 重排”三级策略，不使用生成式图像模型绘制汉字。
- 开放爆炸框、极小字、低覆盖候选一定保留 OCR fallback；摄影页不会因为勾选“跳过 OCR”而真的跳过完整性检查。
- `23/23` 自动测试通过；真实 5 页摄影回归详见 `docs/PHOTO_PAIR_V08.md`。

## v0.8.17 工作台与配对 UI

- 所有预览视图按当前 `selected_index` + 当前页 `page_id` 解析，不再使用跨页的全局最后结果。
- 最终结果、复核标注、蒙版都支持和原图一起翻页；缺失输出会明确留空。
- “优先名称 / 页码配对”“优先文件夹自然顺序”默认关闭，并拉开布局间距。
- 参考 KCC-Kindle-CHS 的卡片、双栏与滚动设置区继续收紧层级和间距；只改 UI/状态解析，不替换处理核心。

## v1.0.2 窄窗口 / 分屏布局

替换工作台在可用宽度不足时自动改为上下布局，右侧参数面板占满可用宽度并仅保留纵向滚动。长下拉项、表单标签和双按钮行不再制造横向最小宽度，因此不会出现右侧内容被窗口裁掉、只能看到半个按钮的问题。

人工补漏现在冻结编辑前结果作为 `manual_effect_base.png`，所有补漏只叠加在该基底上；撤销最后一个补漏区域时恢复该基底，避免其它已经成功替换的区域回退到日文。
## v1.0.5 替换策略

工作台“替换策略 / 蒙版参数”新增 **启用出版安全门禁（关闭 = 激进替换优先）**。默认关闭。关闭后 Direct/Mask 的出版级质量阈值只做诊断，不再阻止可执行的文字替换；如需恢复旧版保守策略，可重新勾选。

