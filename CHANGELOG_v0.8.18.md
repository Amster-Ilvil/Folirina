# v0.8.18

## 页面管理重构

- 将“配对”与“是否处理”拆成两个独立状态：页面可以正确配对，但仍被标记为封面/扉页/目录/章节首页/插图并整页跳过。
- 新增页面类型：正文 / 需替换、封面、扉页 / 书名页、目录、单话首页 / 章节页、插图 / 纯图片、卷首插画、空白页、后记 / 版权 / 广告、手动跳过，以及自动状态“无气泡/文本框”。
- 页面表格支持多选批量标记、恢复自动、自动检查所选；类型颜色参考 Novel Formatter 的页面类型色彩分层。
- 页面标记持久化到 `page_management.json`，重新配对/重新打开输出目录时恢复同一页面的标记。

## 无气泡 / 文本框自动跳过

- 新增 OCR-free Page Manager probe：下采样 OpenCV 配准 + paired-diff 几何检查。
- 高置信度配准且没有 speech balloon / narration text-box 区域时，自动标记为跳过。
- 只有 free-text / splash 文本而没有气泡/文本框时也按整页资产处理；用户可手动改回“正文 / 需替换”。
- 配准不足、几何检测异常或没有可靠证据时默认继续处理，禁止静默漏页。
- 跳过页不运行 OCR、不清字、不迁移，但将高清日文页原样写入最终目录并保留 PageProject/工程元数据。

## 停止任务

- 顶部工具栏新增常驻红色“■ 停止”按钮，右侧处理卡保留“停止处理”作为第二入口。
- 页面自动检查、单页处理、整册处理均支持 cooperative cancellation。
- 单页处理新增多个安全取消边界，停止不再只对整册“下一页之前”生效。
- 已完成页面与最终结果保持不动，停止不会清理或破坏已有输出。

## 断点续跑与状态修复

- 页面管理状态写入每页工程元数据。
- 断点命中前校验最新页面标记：从“正文”改为“跳过”时不会复活旧迁移结果，从“跳过”改回“正文”时不会复活旧 passthrough。
- 修复跳过页 resume fingerprint 缺失问题。
- 表格批量状态列适配新增“跳过”状态，并改为按目标文件名定位行，避免取消/缓存导致行号错位。
