# v0.8.23 — 锁定栅格气泡迁移

本版针对“黑白旧中文版与彩色日文版页面结构相同，但文字迁移后仍出现缺字、歪字、日文残留”的核心问题调整最终栅格几何。

## 核心变化

- 新增 **Rigid Container Raster Transfer（锁定气泡栅格迁移）**。页面级配准仍可使用 affine / homography 来发现对应区域，但最终中文字不再继承 affine 的 X/Y 非等比缩放、剪切或局部光流形变。
- 对安全的白色对白气泡、旁白框和白底文本框：
  - 以目标高清页的完整容器作为清除边界；
  - 保留目标气泡轮廓/尾巴；
  - 清空容器内部的目标日文；
  - 从**原始黑白中文版**提取完整中文字栅格场；
  - 只允许一个统一缩放系数 + 平移，禁止 X/Y 独立拉伸和 shear；
  - 字、标点、抗锯齿边缘作为一个连续栅格整体迁移，不拆成字符组件重新拼装。
- 对结构候选边缘只补充“字符尺寸”的目标残留清除，不对相邻白色画面做 flood-fill，避免气泡尾巴或白色格子把清除区泄漏到整页。
- 彩色/黄色爆炸框不复制黑白背景；继续保留彩色目标框，只替换其中的中文字墨迹。
- 开放文字 / 复杂文字候选增加**发布前内容完整性闸门**：若内容检查不完整，候选不会写进最终页，而是保留为可复核候选，避免把残缺/半擦除结果当成成品。
- `applied` 继续只表示几何写入；最终成功要求 `content_complete=true`。

## 真实样本回归

以 OCR=none 直接验证旧中文版像素迁移本身：

- 065：3 个白色对白气泡走 `rigid_uniform_container + locked-source-raster`，2 个黄色爆炸框走饱和容器文字路线；5/5 内容完整。
- 066：列车小气泡、右侧气泡、中间竖排气泡、左下大气泡全部走锁定栅格路线；4/4 内容完整。

这两个样本页面的全局最佳配准本身仍是 affine；v0.8.23 只把 affine 用于“找位置”，不再用它直接扭曲中文字像素。
