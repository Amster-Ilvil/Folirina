# v0.8.17

## 替换工作台同步

- 新增按页 `PageWorkspace` 解析器，日文原图、旧中文版、最终结果、复核标注、替换蒙版统一绑定当前 `PagePair`。
- 删除工作台对全局 `last_result_path` 的预览依赖，切页时绝不借用上一页结果。
- 新增“上一页 / 下一页 / x / y”工作台导航；切换视图后继续保持同一页。
- 当前页没有最终图、复核图或蒙版时清空预览并提示“本页尚无该输出”。
- 页面表格选择与工作台翻页双向同步。
- 单页任务完成时，即使处理中用户点击过其它表格行，也自动回到真正完成的页面再显示结果。

## 整册与复核结果

- 整册任务完成后缓存 `BookProject.pages` 的逐页工程对象，断点命中页也可直接浏览。
- 同时支持从输出目录 `pages/<page_id>/project.json`、`qa.json` 和页面工件恢复工作台，因此重用缓存/断点输出不会依赖“最后处理页”。
- `final_reviewed.png` 在工作台中优先于旧 `final.png`。
- 工作台执行人工补字/接受候选/还原日文后，会把 reviewed final 同步回整册 `final/<target-stem>.png`，避免出版目录仍保留旧结果。

## 页面配对 UI

- “优先名称 / 页码配对”默认关闭。
- “优先文件夹自然顺序”默认关闭。
- 两个选项增加 30 px 水平间距和独立说明，默认行为明确为智能视觉配对。
- `PairingConfig` 与 `config.example.json` 默认值同步为关闭；显式启用时原来的名称/顺序快速路径保持不变。

## CJK 文件名稳定性

- 修复 `stem_id()` 只接受 ASCII 导致纯中文/日文页名可能全部映射到 `page` 的目录冲突。
- 现在保留 Unicode 字母/数字，只清理不安全标点和空白。
- 单页 Worker 与整册 Worker 统一使用相同 canonical page id。
- 工作台仍兼容 v0.8.16 单页处理使用原始 `Path.stem` 创建的旧页面目录，也会只读探测旧版 ASCII-only 批量 page id。

## 批量状态修复

- 修复整册处理刚进入“正在处理”阶段时，表格因页序号非零而提前显示“完成”的旧状态错误；现在处理中 / 缓存 / 完成 / 失败 / 已取消与实际进度一致。

## UI

继续参考 KCC-Kindle-CHS 的 macOS 卡片式 UI：更统一的 13 px 基础字号、21 px 顶部标题、分段式工作台视图按钮、右侧滚动参数区和更清晰的卡片间距。迁移 Pipeline、精准蒙版、glyph-footprint rescue、OCR/配准核心没有为了换 UI 被重写。
