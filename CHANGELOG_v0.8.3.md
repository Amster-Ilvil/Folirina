# v0.8.3

## 摄影源边缘裁切完整性保护

- 修复 v0.8.2 会把“被手机照片边缘裁掉的中文气泡”错误计为成功的问题。
- 新增 `photo_pair_edge_clip_guard_enabled`：源气泡触碰照片边缘时启用更严格的完整覆盖检查。
- 新增 `photo_pair_edge_clip_min_target_coverage`（默认 `0.94`）。低于该值时不再清空高清日文气泡，也不会发布残缺中文。
- 新增 `source_edge_clipped` / `source_edge_sides` 诊断字段，输出 JSON 可直接定位左/右/上/下边缘裁切。
- 新增 blocking QA：`mask_replace_source_translation_clipped`，明确指出“输入源本身缺字”，避免假成功。
- 保留边缘轻微裁到气泡轮廓但文字完整的情况：只要目标覆盖率达到严格门槛仍正常替换。
- 真实 `009` 回归：右上源气泡在照片右边缘物理截断，v0.8.3 正确拒绝该 1 个区域并保留日文原气泡；其余 7/7 继续用 `photo-crisp-ink` 成功替换，其中右下同样触边但覆盖率 `0.9889` 的小气泡正常通过。
- 真实 `007` 回归：4/4 继续成功，0 error。
- 自动测试：33/33 通过。

> 注意：当中文照片本身没有拍到完整译文时，任何纯像素转移算法都无法恢复不存在的字符。v0.8.3 的目标是绝不把这种情况伪装成“替换成功”；应补拍完整中文版，或在 Review/OCR 流程中人工补齐文字后再发布。
