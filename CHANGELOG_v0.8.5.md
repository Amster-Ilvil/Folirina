# v0.8.5 (2026-08-11)

## 目标
继续提高摄影版旧汉化 → 高清日文母版的实际替换覆盖率，同时不放宽 v0.8.4 的“残缺源禁止假成功”安全规则。

## 主要改进

### 摄影主路线 + v0.8 结构差分补漏
- `photo_pair` 继续作为主检测/迁移路线，负责封闭对白气泡、摄影源完整性、边缘裁切保护和高清墨迹重建。
- v0.8 结构差分不再只在 photo_pair 完全失败时运行；现在可作为 **supplement** 检测器补充开放爆炸框 / free text。
- 补充区域必须满足：
  - 与 photo_pair 已确认区域不重叠、不邻接；
  - 目标区域足够明亮、具有足够变化像素；
  - 排除页面底部页码/版权噪声；
  - 只对筛选后的漏区执行 target-driven transfer。
- 这样不会覆盖已经正确的主路线结果，也不会绕过右边缘裁切保护。

### 开放文字也使用高清墨迹重建
- supplement 来自摄影页时，不再直接复制局部摄影像素。
- `bubble` 与 `free_text` 都优先进入 `photo-crisp-ink`，在目标高清纸面上重建本地墨迹，降低模糊与灰底。

### OCR 高清重排方向推断
- `lettering.orientation` 默认改为 `auto`。
- 高清重排时优先依据 **源 OCR 文字块几何** 判断竖排/横排，而不是仅看近似圆形气泡的长宽比。
- 适合日漫中“圆形气泡 + 竖排中文”的常见情况。

### GUI 待补文字
- 替换工作台新增 **待补文字** 卡片。
- 对 `source_text_region_clipped_at_page_edge`：
  - 自动显示待补气泡；
  - 可输入完整中文；
  - 可选择自动 / 竖排 / 横排；
  - 点击“补字并生成复核图”后直接执行高清清底 + reletter；
  - 不再要求用户手工编辑 JSON。
- JSON 模板仍保留，便于批量/自动化工作流。

## 回归测试
- pytest：38/38 通过。
- 007 真实摄影页：主 photo_pair 4 区 + structural supplement 2 区，共 6/6 已检测区域成功替换，0 error / 1 warning；彩色拟声词实验迁移默认关闭，避免摄影阴影造成字形破损。
- 009 真实摄影页：7 个完整区域正常替换；右上 1 个物理裁切源继续安全阻断，未被 supplement 绕过，并进入 GUI/manual reletter 队列。
