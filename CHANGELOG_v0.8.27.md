# Manga-HD-Translation-Transfer v0.8.27

## 重点修复

本版把同版黑白中文 → 彩色日文的**白色气泡 / 文本框**切换为**整容器覆盖**路线：

- 不再只提取中文字墨迹。
- 改为从中文版提取**整个白色容器内部**（中文字 + 周围白底）
- 通过现有页面配准 + 局部统一缩放 + 平移，直接覆盖到日文页对应容器内部。
- 目标容器仍然是几何真值，避免跨版轻微尺寸差导致内容被裁掉。

## 实现变化

- `rigid_container_full_patch_enabled = True`（默认开启）
- 新增 `rigid-container-full-patch` 后端
- 新增 `locked-source-container-patch` 清晰度标记
- 保留旧 `rigid-container-raster` 作为回退
- 白色容器路径继续使用 `solid_container_interior`，先补齐内部孔洞再处理
- 目标写入区域可保留 1px 边线，避免覆盖掉原气泡黑边的抗锯齿

## 预期收益

- 不再出现“日文没清干净但中文已经写进去”的混合残留
- 不再因为目标字洞/源字洞把中文裁残
- 中文不重排、不 OCR 重写，继续保持**原始中文字形**
- 更符合“把整个中文版气泡/文本框贴到日文版上”的实际诉求
