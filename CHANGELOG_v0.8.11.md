# v0.8.11

## 真实 Mac 排版质量修复

- Apple Live Text 只负责识别 Unicode；不再无条件覆盖已经清晰、版式正确的旧中文版字形。
- 新增“清晰旧中文版保留原字号/分列（推荐）”：默认开启。mask transfer 已重建出清晰中文时，OCR 仅作为证据/复核，不触发二次排版。
- 只有摄影模糊、低相对清晰度、候选待复核或像素转移失败的区域才自动升级为 OCR 高清重排。
- OCR 重排会从旧中文版隔离后的中文字形估算字高、列数和方向，字号搜索限制在源排版附近，避免短句从 72px 开始被放大成海报字。
- transcript-only OCR 的目标清字不再用整个气泡/矩形作为清除 mask；改为在目标气泡安全区内检测日文字形。
- 当 reletter base 已经包含先前迁移的中文字形时，同时清除目标日文和旧中文候选，修复黑色鬼影/双字。
- OCR 排版失败时自动恢复原中文候选，不再留下空白气泡。
- 新增排版提示、synthetic target ink mask、清晰源排版保留的回归测试。

## 验证

- 真实用户 006 黑白中文 → 彩色日文页：6/6 区域应用。
- 模拟 Apple Live Text transcript 存在时：6/6 OCR 区域被识别，但 0 个清晰区域被二次重排，输出与无 OCR 的高质量源排版迁移逐像素一致。
- 旧 007 回归：6 applied，0 error / 1 warning（Linux 无 Apple Live Text）。
- 旧 009 回归：8 applied；页边裁切候选仍进入复核，不回退为日文。
- pytest 56/56 通过。
