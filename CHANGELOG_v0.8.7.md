# v0.8.7 (2026-08-11)

## 黑白中文版 → 彩色/不同版本日文母版专项修复

### 1. 新增跨色彩版本 target-driven 路线
- 自动识别“低饱和/黑白中文版 → 彩色目标页”。
- 对这类页面，不再要求源气泡轮廓和目标气泡尺寸严格一致；以高清目标页气泡为唯一写入边界，在目标坐标恢复中文版文字。
- 解决真实 `006` 样本中 `target_bubble_much_smaller_after_registration` 与低 IoU 导致的整块拒绝。

### 2. 放宽但严格筛选的目标气泡候选
- 新增 registered-ink relaxed candidates：当源/目标轮廓不同，但目标是可靠白色文字容器、两侧均存在文字墨迹且文字结构差异足够时，允许作为可复核中文候选。
- 真实 `006`：目标容器 12 个，最终接受 6 个对白区域，其中 3 个来自该补充路线。
- 通过目标暗部密度、边界完整度、源/目标墨迹密度与变化度联合门控，避免把屋顶窗、桌面等画面结构误识成对白。

### 3. 小气泡中文字整块重新居中
- 黑白源中完整中文往往与日文排字位置不同；旧逻辑只在目标 mask 原位置采样，会截掉外侧笔画或混入日文。
- v0.8.7 对小型白色气泡从目标周边收集完整的紧凑中文字形，过滤气泡边线/画面长线，再以**单一等比例缩放**居中到目标安全区。
- 不做 X/Y 独立缩放，避免字符压扁和扭曲。

### 4. 清除日文残字/灰色鬼影
- 扩展目标清字 mask：把紧贴气泡内部、但因检测边界不完整而落在 mask 外的紧凑日文字形也纳入清除。
- 同时包含 1px 抗锯齿边缘，避免出现“中文已经替换但上方还残留淡灰日文”的混字问题。
- 长气泡轮廓、面板线与画面结构不会被该规则清除。

### 5. 保持旧摄影页策略
- target-driven 跨版本路线只自动用于“黑白/低饱和中文版 → 彩色目标”场景。
- 普通彩色摄影中文版继续走 v0.8.6 的直接迁移 + 低置信候选 + 结构补漏，避免新策略降低旧样本质量。
- 小气泡重新居中也限制在低饱和源，摄影彩页不强制使用。

## 实际回归
- 新 `006` 黑白中文 → 彩色日文：上一版 `3 applied / 3 rejected / 4 QA errors`，本版 `6 applied / 0 rejected / 0 QA errors / 1 OCR warning`。
- 旧 `007`：`0 QA errors / 1 warning`，保持可用。
- 旧 `009`：8 个区域先给中文，其中 2 个页边裁切候选继续作为 blocking review 项，可编辑/可还原，不回退为日文。
- pytest：42/42 通过。
