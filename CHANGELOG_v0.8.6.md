# v0.8.6 (2026-08-11)

## 重点修复

### 1. 低置信区域不再直接留日文
- 摄影中文版在页边被裁切、覆盖不足但仍有可用中文字形时，默认先生成 **中文候选**。
- 候选会清除目标气泡内的旧日文，避免出现“中文失败就直接露出日文”的混合页。
- 这类区域写入 `candidate=true / review_required=true / restorable=true / editable=true`，仍作为出版阻断项，必须复核后才能视为正式成品。

### 2. 复核标注与可逆操作
- 新增 `review_preview.png`：只用于复核，不污染干净的 `final.png`。
- 两类提示：
  - `中文版候选 · 可能不完整/不准确 · 可编辑/还原`
  - `摄影中文字形 · 可能模糊/扭曲 · 可编辑/还原`
- GUI 替换工作台新增 **复核标注** 视图。
- 每个待复核区域支持：
  - 接受当前中文候选
  - 重新编辑并用高清字体排字
  - 一键还原日文原图
- `review_overrides.json` 新增：
  - `accept_candidate_targets`
  - `restore_target_bubbles`
  - 继续支持 `manual_reletter`

### 3. 防止中文被局部拉伸扭曲
- 摄影页局部 bbox 拟合不再默认对 X/Y 独立缩放。
- 改为 **等比例局部缩放 + 中心平移**，并保留 ECC 平移细调。
- 当 X/Y 所需缩放差异过大时直接保留全局配准，避免把 CJK 字符压扁或拉长。

### 4. 高清重排抗锯齿改进
- `LetteringConfig.supersample_factor=4`。
- 中文排字先在 4× 分辨率渲染，再用 Lanczos 一次缩回目标尺寸。
- 最终页面本身从不整体 resize，减少小字号边缘发糊和锯齿。

### 5. OCR 不可用时也明确提示摄影字形风险
- 当摄影页只能使用 `photo-crisp-ink` / 摄影字形迁移时，区域进入 `review_regions`。
- 这类区域不会被假装成完全确认的高清排字，可以直接在 GUI 中编辑或还原。

## 回归与真实样图
- pytest：40/40 通过。
- 007：6 个区域实际替换；其中摄影字形区域进入可编辑/可还原复核列表；0 error / 1 warning（当前 Linux 测试环境无 OCR）。
- 009：8 个检测区域均先得到中文输出；其中 2 个页边裁切/覆盖不足区域为低置信中文候选，不再直接留日文；生成复核标注图。
- 实测一键还原 009 `photo-dst-001`：目标 bbox 与原始日文母版逐像素一致。
