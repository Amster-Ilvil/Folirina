# v0.8.13

## 智能配对升级

- GUI 新增两个独立选项，默认开启：
  - **优先名称/页码**
  - **优先文件夹顺序**
- 两项同时开启时的配对顺序：
  1. 唯一同名 / 归一化同名 / 唯一页码直接锁定；
  2. 名称锚点之间若两侧剩余页数相同，则按自然排序直接配对；
  3. 只有仍未解决的区间才计算 dHash、边缘结构、宽高比和顺序成本进行视觉智能补漏。
- 支持 Finder/浏览器副本后缀归一化，例如 `006(2).jpg` 与 `p-006(1).jpeg` 会识别共同页码 `006`。
- 支持常见版本后缀归一化：`_cn/_zh/_chs/中文/汉化` 与 `_jp/_ja/_raw/日文/原版`。
- 名称锚点必须唯一且保持整册单调顺序；重复页码或冲突名称不会静默强配。
- 顺序优先不会在页数不等的区间硬配，避免一张插页让后续整本错位；该区间自动交给视觉智能配对。
- 能被名称/顺序锁定的页面不再读取整图计算视觉指纹，整册配对速度显著降低 I/O。
- 配对表现在显示 **名称 / 顺序 / 智能** 来源与置信度，状态栏汇总各类数量。

## 文本完整性

- 继承 v0.8.12 的“清晰但残缺”保护：保留旧中文版排版前比较源/目标列数、行数、墨迹占比与字距；检测到列缺失/首字缺失时强制进入 OCR 高清重排。

## 验证

- 全量 pytest 通过。
- selftest 通过。
- 真实 `006(2).jpg → p-006(1).jpeg`：
  - 名称优先：按页码 `006` 直接配对；
  - 顺序优先：直接按自然排序配对；
  - 两项同时开启：名称优先命中；
  - mask_replace 实际处理完成，0 QA error / 1 OCR 环境 warning。
