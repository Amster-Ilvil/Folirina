# v0.8.33

## 目标感知合成与颜色污染防护

- 对 `bubble` 区域增加目标画面高饱和容器识别；彩色星形气泡、爆炸框等不再被当作白色气泡覆盖。
- 这类区域改为“保留高清目标底色，只转移源中文墨迹”的合成方式。
- Source-Direct 发现疑似画面内容候选时，不再凭借部分成功结果直接发布；自动回退到目标感知 paired recovery。
- 诊断信息新增 `safe_blocked_by_artwork_rejections`，便于复核为什么没有使用快速路径。

