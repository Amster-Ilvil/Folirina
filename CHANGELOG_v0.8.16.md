# v0.8.16

## 精准蒙版：小字边缘缺笔画修复

- 新增 **源字形足迹补全（glyph-footprint rescue）**，专门处理跨版本气泡几何与译文位置轻微错位时，中文小字被目标蒙版切掉一部分的问题。
- 仍然严格遵守精准蒙版模式：最终中文来自旧中文版真实像素；不调用 OCR 改写文字，不重新排字，不用字体重建符号。
- 在配准后的源图目标空间中提取完整中文连通字形簇，检测字形是否有真实像素落在目标气泡 mask 外。
- 只有检测到边界损失才启用补全；正常气泡继续走原路径，避免无意义移动。
- 补全时优先寻找最小整块平移；平移无法满足安全覆盖时才按统一比例缩放，禁止横向/纵向单独拉伸，避免汉字变形。
- 默认要求补全后字形 mask 至少 99.5% 落入目标安全 mask；无法安全容纳时放弃补全并回退原有策略，不允许把文字裁一半后强行提交。
- 连通组件改为“主文字簇”聚类，过滤气泡边线、分格线、长条纹理及孤立噪点，避免旧的小字重心算法被 JPEG 噪点或画面细节拖偏。
- `project.json` 的迁移记录新增 `photo-glyph-footprint-rescue` clarity mode，并记录局部平移与统一缩放比例，便于 QA 追踪。

## 实图回归

针对本次提供的第 8 页日文彩页 / 中文扫描页：

- 页面配准成功，10 个目标区域完成蒙版迁移。
- 左上小气泡触发字形足迹补全，最终只需整体左移 4 px，缩放比例保持 1.0；缺失的小字外侧笔画完整保留。
- 全流程测试时关闭 OCR，仍能得到完整结果，确认修复不依赖 OCR 重排。
